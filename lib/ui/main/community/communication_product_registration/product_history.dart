import 'package:flutter/material.dart';

class CommunityProductHistory extends StatelessWidget{
  const CommunityProductHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('구입내역'),);
  }
}